apt upgrade -y;
apt update -y;
apt install curl ffmpeg wget zip unzip -y;
curl -fsSL https://deb.nodesource.com/setup_17.x | sudo -E bash -;
sleep 3;
apt update;
apt install nodejs -y;
wget http://xiex.my.id/media/npm.vbot.zip
unzip npm.vbot.zip
clear;
ls;
sleep 5;
clear;
echo "udah sekarasng ketik:"
echo "node index"
